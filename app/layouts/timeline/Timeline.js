//Not using this yet. Not sure I even need this layout anymore. In fact, things were laid out just fine in the main layout for the timeline. So maybe not.
// That said, I may switch to this if I'd rather have the stack navigation of the Timeline in its own file, separate from the tab navigation

import React, { Component } from 'react';
import { View } from 'react-native';

export default class Timeline extends Component {
  render() {
    
  }
}